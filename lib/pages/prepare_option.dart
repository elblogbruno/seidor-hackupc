import 'package:flutter/material.dart';

class PrepareOption extends StatelessWidget {
  const PrepareOption({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('prepare_option'),
      ),
      body: const Center(
        child: Text('Bienvenido a la Cuarta Pantalla'),
      ),
    );
  }
}